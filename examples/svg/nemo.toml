# Project manifest for the SVG component demo.
#
# An app project (full app.nemo, no [package] table). Launch it with
# `nemo --app-config examples/svg`.
name  = "svg"
entry = "app.nemo"

[build]
out  = "dist"
load = "source"
