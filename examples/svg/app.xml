<nemo>
  <app title="SVG Demo">
    <window title="SVG Component" width="640" height="520">
      <header-bar theme-toggle="true" />
    </window>
    <theme name="nord" mode="dark" />
  </app>

  <script src="./scripts" />

  <layout type="stack">
    <label id="title" text="SVG Component" size="xl" />

    <panel id="embedded_panel" title="Embedded (click to recolor + rotate)" padding="16">
      <!-- Standard SVG syntax embedded directly in the config. The parser
           captures this subtree verbatim and the component rasterizes it. The
           on-click handler rewrites the whole `content` markup to restyle it. -->
      <svg id="embedded" width="120" height="120" viewBox="0 0 100 100" on-click="on_svg_click">
        <defs>
          <linearGradient id="grad" x1="0" y1="0" x2="1" y2="1">
            <stop offset="0%" stop-color="#bf616a" />
            <stop offset="100%" stop-color="#ebcb8b" />
          </linearGradient>
        </defs>
        <rect x="5" y="5" width="90" height="90" rx="16" fill="url(#grad)" />
        <circle cx="50" cy="50" r="26" fill="none" stroke="#2e3440" stroke-width="6" />
        <text x="50" y="58" font-size="24" text-anchor="middle" fill="#2e3440">N</text>
      </svg>
    </panel>

    <panel id="file_panel" title="From file (src=...)" padding="16">
      <svg id="from_file" src="assets/rust-logo.svg" width="120" height="120" />
    </panel>
  </layout>
</nemo>
